# Bitácora de Uso de IA - Semana 1

## Entrada 1: 2026-07-13
* **Prompt enviado a la IA:** "Ayúdame a modelar una estructura para lecturas de sensores usando buenas prácticas de Python idiomático. Necesito representar tipos de sensores, estados de alerta, asegurar inmutabilidad y definir 5 funciones puras de procesamiento, además de un Protocol."
* **Código generado por la IA:** Propuso el uso de `Enum` para los tipos y estados, una `@dataclass(frozen=True)` para la lectura, y funciones que utilizan `dataclasses.replace` para mantener la pureza, junto con `typing.Protocol`.
* **Decisión de diseño y justificación:** Acepté el diseño completo porque soluciona de forma elegante el problema de la mutabilidad que sufríamos en sistemas embebidos convencionales. Mantener la lectura `frozen` previene efectos secundarios indeseados en canalizaciones de datos. El uso de `Protocol` permite la inyección de dependencias modular que se requerirá más adelante en la semana para SOLID sin la rigidez de la herencia múltiple de C++.
* **Detalle personal:** Aunque se que comento que esto solo es para practicar lo guardo aqui para tener a la mano lo que estudie y si lee esto y le es posible, le agredeceria que tambien me pudiera dar una retroalimentacion de este ejercicio de practica.

## Entrada 2: 2026-07-14
* **Prompt enviado a la IA:** "Con las ideas que te adjunte genera el código para fsm_demo.py y test_fsm.py basandote en ellas. La máquina debe ser orientada a objetos, usar un método explícito llamado transition(), ciclar entre RED, GREEN y YELLOW, y contar las vueltas completas. Además, incluye los 4 tests requeridos."
* **Código generado por la IA:** Diseñó la FSM usando una clase con propiedades de solo lectura para encapsular el estado y el contador, protegiendo las variables internas, junto con funciones de test independientes basadas en aserciones de pytest.
* **Decisión de diseño y justificación:** Se eliminó la idea de abstracción de las propiedades protegidas para evitar sobrecomplicar el diseño de software en esta etapa. En Python, el uso de atributos públicos es idiomático si no se requiere validación extra en la lectura. Esto mantiene el código limpio, plano y 100% defendible ante el coordinador, conservando la eficiencia de la tabla de búsqueda (diccionario) para las transiciones.

## Entrada 3: 2026-07-15
* **Prompt enviado a la IA:** "Ayúdame a optimizar el archivo solid_srp_ocp_lsp.py y su test correspondiente. Necesito que revises los ejemplos claros de 'Mal' y 'Bien' para los principios SRP, OCP y LSP aplicados al dominio de sensores, con 2 tests unitarios por principio."
* **Código generado por la IA:** Diseñó estructuras que separan el almacenamiento de la analítica (SRP), clases abstractas extendidas por polimorfismo para las alertas (OCP) y subclases que respetan los tipos de retorno numéricos sin romper el contrato del padre (LSP).
* **Decisión de diseño y justificación:** Se optó por el polimorfismo clásico de Python para OCP y LSP. Esto elimina la necesidad de modificar lógica existente cuando ingresa nuevo hardware al sistema, mitigando los riesgos de regresión comunes en el firmware tradicional.

## Entrada 4 : 2026-07-16
* **Prompt enviado a la IA:** "Revisa y optimisa el archivo solid_isp_dip.py y test_solid_isp_dip.py enfocado en los principios ISP y DIP usando typing.Protocol para inyección de dependencias en instrumentación."
* **Código generado por la IA:** Creó interfaces segregadas (ReaderProtocol y WriterProtocol) para cumplir con ISP, y un DataProcessor de alto nivel que recibe la interfaz por el constructor para cumplir con DIP.
* **Decisión de diseño y justificación:** El uso de `typing.Protocol` permite implementar desacoplamiento por Duck Typing estático. Al inyectar el protocolo en el constructor de la clase de alto nivel, el código queda 100% aislado del driver físico subyacente, facilitando el intercambio de hardware o la creación de Mocks en las pruebas sin alterar la lógica de negocio.
## Entrada 5: 2026-07-17
* **Prompt enviado a la IA:** "Ayúdame a optimizar el diseño de mi módulo config.py y su test unitario para un paquete uart_driver. Recuerda que debe usar una dataclass frozen y validar baudrates estándar de electrónica."
* **Código generado por la IA:** Modifique la clase UartConfig usando __post_init__ para la validación de un set de enteros y generó pruebas para verificar la inicialización, la excepción de error y el bloqueo por FrozenInstanceError.
* **Decisión de diseño y justificación:** El uso de una dataclass inmutable (frozen=True) simula los registros de configuración de un microcontrolador que no deben alterarse arbitrariamente tras el encendido. Colocar la lógica de validación en __post_init__ atrapa errores de configuración en tiempo de desarrollo antes de que interactúen con el dispositivo virtual.
## Entrada 6: 2026-07-18
* **Prompt enviado a la IA:** "Ayudame a terminar de diseñar el archivo parsers.py y su correspondiente test para el paquete uart_driver. Requiero que agregres una clase abstracta MessageParser y dos subclases concretas: ModbusParser y NMEAParser que cumplan con los principios OCP y LSP."
* **Código generado por la IA:** Diseñó la estructura con la biblioteca abc usando métodos abstractos para tipar bytes de entrada y retornar diccionarios estructurados, agregando aserciones de error controladas (ValueError).
* **Decisión de diseño y justificación:** El uso de Clases Bases Abstractas (ABC) proporciona validación en tiempo de diseño estructural. Separar la lógica de Modbus (binario) y NMEA (ASCII) en objetos autónomos en lugar de usar un switch condicional centralizado disminuye drásticamente el acoplamiento del software y simplifica el aislamiento en las pruebas unitarias.
## Entrada 7: 2026-07-18
* **Prompt enviado a la IA:** "Siguiendo las ideas que di del archivo device.py y su test unitario correspondiente para la clase UartDevice, ayudame a implementar la inyección de dependencias para UartConfig y MessageParser cumpliendo con el principio DIP."
* **Código generado por la IA:** Diseñó la clase UartDevice controlando banderas de conexión e implementando el método process_incoming_stream que delega el comportamiento al parser mediante polimorfismo, lanzando excepciones controladas.
* **Decisión de diseño y justificación:** Aplicar Inversión de Dependencias (DIP) en el constructor del dispositivo evita que el módulo UART quede rígidamente acoplado a un protocolo en específico. Esto permite simular fallos del bus físico de datos (puerto cerrado) de forma independiente y probar el flujo completo con múltiples parsers sin modificar la clase base.
## Entrada 8: 2026-07-18
* **Prompt enviado a la IA:** "Ayudame a configurar el sistema de almacenamiento del archivo recorder.py y su test unitario para la clase DataRecorder. El almacenamiento debe implementar el formato JSON-lines (.jsonl) usando operaciones append-only."
* **Código generado por la IA:** Creó la clase DataRecorder con un método que serializa diccionarios vía json.dumps y escribe saltos de línea explícitos, implementando pruebas con la herramienta tmp_path de pytest para aislar el entorno de E/S.
* **Decisión de diseño y justificación:** El uso de JSON-lines separa por completo la lógica de almacenamiento de la de comunicación (SRP). Este formato optimiza el uso de memoria RAM del sistema al evitar operaciones de lectura/escritura destructivas globales en disco, ofreciendo una solución escalable y tolerante a fallos de energía durante la captura de telemetría.
## Entrada 9: 2026-07-18
* **Prompt enviado a la IA:** "Verifica errores en estos comandos para hecer una ejecución del control de calidad estático final de la semana 1 utilizando ruff y mypy, resolviendo conflictos de mapeo de rutas de módulos."
* **Código generado por la IA:** Ninguno. Se asistió en la instalación local de herramientas de auditoría en Python 3.14 y en el uso del parámetro --explicit-package-bases para corregir el análisis de tipos.
* **Decisión de diseño y justificación:** La integración de linters (ruff) y analizadores de tipos estáticos (mypy) en la etapa de cierre asegura la integridad estructural del software antes del despliegue o entrega. Forzar bases explícitas en Mypy garantiza que los entornos de CI/CD mapeen los paquetes de forma idéntica al entorno de ejecución local.